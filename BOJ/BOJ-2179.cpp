#include <bits/stdc++.h>
using namespace std;
int main(){
    int N;
    cin >> N;
    int max_len = 0;
    vector<string> arr(N);
    for(int i=0; i<N; i++) cin >> arr[i];
    unordered_map<string, int> mp;
    for(int i=0; i<N; i++){
        
    }
}